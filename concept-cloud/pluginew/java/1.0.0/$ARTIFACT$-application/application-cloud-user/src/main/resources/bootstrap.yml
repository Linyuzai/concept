server:
  port: 10000
spring:
  application:
    name: $ARTIFACT$-application-cloud-user
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/$ARTIFACT$?serverTimezone=GMT%2b8
    username: root
    password: 123456