dependencies {
    api project(':$ARTIFACT$-login')
    api project(':$ARTIFACT$-domain:domain-sample')
    api project(':$ARTIFACT$-domain:domain-user')
    implementation 'org.springframework.cloud:spring-cloud-loadbalancer'
}